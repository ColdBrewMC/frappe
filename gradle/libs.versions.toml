[versions]
# The latest versions are available at https://lambdaurora.dev/tools/import_quilt.html
# https://fabricmc.net/develop/ for fabric

# Base
minecraft = "26.1-snapshot-5"
fabric_loader = "0.18.4"

# Libraries
fabric_api = "0.142.3+26.1"
jspecify = "1.0.0"

# Mod Integrations

# Runtime Mods
modmenu = "18.0.0-alpha.5"

[libraries]
# Base
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
fabric_loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric_loader" }

# Libraries
fabric_api = { module = "net.fabricmc.fabric-api:fabric-api", version.ref = "fabric_api" }
jspecify = { module = "org.jspecify:jspecify", version.ref = "jspecify" }

# Mod Integrations

# Runtime Mods
modmenu = { module = "com.terraformersmc:modmenu", version.ref = "modmenu" }

# If you have multiple similar dependencies, you can declare a dependency bundle and reference it on the build script with "libs.bundles.example".
[bundles]

[plugins]
fabric_loom = { id = "net.fabricmc.fabric-loom", version = "1.15-SNAPSHOT" }
extra-java-module-info = { id = "org.gradlex.extra-java-module-info", version = "1.13.1" }
yumi-gradle-licenser = { id = "dev.yumi.gradle.licenser", version = "2.2.+" }
